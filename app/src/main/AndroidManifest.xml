<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quanylysinhvien">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/iconapp"
        android:label="SINH VIEN MANAGER"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".InFoAppActivity"></activity>
        <activity android:name=".loginandregisteractivity.LoginActivity" />
        <activity android:name=".loginandregisteractivity.ChangePasswordActivity" />
        <activity android:name=".loginandregisteractivity.RegisterActivity" />
        <activity android:name=".introActivity.IntroOneActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ThemLopActivity" />
        <activity android:name=".DanhSachLopActivity" />
        <activity android:name=".ManagerActivity" />
        <activity android:name=".ThemSinhVienActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/AppTheme" />
    </application>

</manifest>